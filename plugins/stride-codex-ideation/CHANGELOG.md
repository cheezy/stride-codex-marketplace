# Changelog

All notable changes to the `stride-codex-ideation` plugin are documented here.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/), and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [0.3.1] - 2026-08-21

Documentation only. The ideation loop, the decomposer, and the plugin's command-skills are unchanged.

### Fixed

- **The README described the default `lean` profile as matching "upstream v0.3.0 behavior".** Defining a profile by a version number of a different plugin asks the reader to reconstruct that release from a changelog before they can tell what the default does. The sentence now describes the profile directly: `lean` runs the shared core only — the seven gated sections plus the mandatory framing checkpoint, premortem, and challenge gate — with no profile-specific forcing questions and no optional document sections.

## [0.3.0] - 2026-07-04

Review-queue hardening pass: the decomposer contract, stridify stamping, batch validator, and calibration fixtures are updated so goals and tasks shipped through this port are fully attributed and score complete against Stride's review queue, plus a sweep of stale documentation references left over from the Codex port.

### Added

- **`created_by_agent` stamping in the stridify pipeline (W1535)** — `skills/stride-ideation-stridify/SKILL.md` Step 8b now stamps `created_by_agent` on every goal object (not the root, not child tasks — the server propagates the goal's value to its nested tasks) before the batch is written and POSTed, so goals shipped through this port are attributed in the `/agents` activity feed instead of showing `?`. `lib/strip_audit_fields.py` deliberately preserves the field (it strips only the three root-level local-audit fields), and both `lib/test-ship-helpers.{sh,ps1}` gain a mirrored regression asserting it survives the strip.
- **`varchar(255)` length check and advisory scored-field warnings in `lib/validate_batch.py` (W1537)** — a sixth fatal check `(f) length_limit` fails (exit 1, naming the JSON path) when a goal/task `title` or a `security_considerations` element exceeds 255 Unicode code points (matching Stride's changeset guard; `pitfalls` / `key_files` JSONB stay unbounded), and an advisory pass warns on **stderr** (exit stays `0`) for any task missing or empty one of the five review-queue scored fields. The five original structural checks are unchanged and still fire in order. `lib/test-validate-batch.{sh,ps1}` gain length + advisory cases (bash 29 / pwsh 19 passing), and the stridify Step 8a check table + validator docstring document the new checks.

### Changed

- **Decomposer contract exemplifies all five scored fields (W1533)** — `agents/requirements-decomposer.md`'s canonical task skeleton now lists `security_considerations`, and one fully-populated task in each of the three worked examples carries both `testing_strategy` and `security_considerations`, so an implementer copying an example emits tasks the review queue scores as complete. The field-format table and "What you MUST NOT emit" allow-list stay consistent (`security_considerations` remains a valid scored field, not a stripped one).
- **Calibration fixtures backfilled (W1534)** — every task across the three `fixtures/*-stride-batch.json` batches (dark-mode 5, notifications 16, replace-test-suite 14) now carries a scope-matched `testing_strategy` object and `security_considerations` array (each element under 255 chars); `fixtures/README.md` notes the fixtures now exercise the two previously-omitted scored fields. `source_spec_sha256` values and dependency ordering are unchanged.

### Fixed

- **Requirements-reviewer profile-aware check-count contradiction (W1536)** — `agents/requirements-reviewer.md` said both "five conditional checks" and "All three checks are advisory"; corrected the stale "three" to "five" so the count is consistent with the five profile-aware bullets (Concrete Example, JTBD-Problem, Why-now, MVP/Validation, Falsifiable).
- **Stale `fixtures/SMOKE-TEST-NOTE.md` (W1538)** — corrected the result count from `10 passed` to `14 passed, 0 failed` (verified against `lib/run_smoke_test.sh`), added the Stage 6 challenge-gate row to the verified-stages table, renumbered the live-POST stage to Stage 7, and replaced the two `Copilot CLI` references with `Codex CLI`.
- **Removed-slash-command references in helper docstrings and test-script comments (W1539)** — rewrote stale `/stride-ideation:decompose`, `/stride-ideation:ship`, and `commands/` references in `lib/test-stamping.sh`, `lib/test-ship-helpers.sh`, `lib/strip_audit_fields.py`, and `lib/filename.sh` to name the current `stride-ideation-ideate` / `stride-ideation-stridify` skill surfaces; the `.ps1` twins already carried the correct wording. Comments/docstrings only — no assertion or helper logic changed.
- **Orphaned drift check + smoke-test self-description (W1540)** — `lib/drift_check.py`'s docstring now documents it as a standalone fixture-regression / audit helper (its only callers are `run_smoke_test.sh` Stage 2 and `test-drift-check.sh`) and states the `stride-ideation-stridify` skill deliberately omits drift checking (it writes and POSTs the batch in the same invocation, so the source cannot drift); `lib/run_smoke_test.{sh,ps1}` no longer describe a removed `/stride-ideation:ship pipeline`.

### Release

No marketplace pin — `stride-codex-ideation` is **not** published on any marketplace (unlike `stride-ideation` on the Claude Code marketplace or `stride-copilot-ideation` on the Copilot marketplace), so this release is a git tag + GitHub release on the `stride-codex-ideation` repo only. No `marketplace.json` or marketplace README is touched.

### Source

W1533 (decomposer skeleton + examples), W1534 (fixture backfill), W1535 (`created_by_agent` stamping), W1536 (reviewer check-count fix), W1537 (validator length + scored-field checks), W1538 (SMOKE-TEST-NOTE refresh), W1539 (slash-command reference corrections), W1540 (drift-check + smoke-test self-description), W1541 (this release).

## [0.2.0] - 2026-06-26

### Added

- **Challenge gate (W1370)** — a new mandatory, profile-independent design stress-test ported from the canonical [`cheezy/stride-ideation`](https://github.com/cheezy/stride-ideation) plugin and adapted to the Codex CLI question/selection primitive (never Claude Code's `AskUserQuestion`). It runs after the round-4 premortem (and the Round-5 MVP-design batch under `lean-startup`) and **before** the reviewer pass, over the assembled draft. Four components, run in order: (a) an **assumption-confidence audit** rating every Assumptions entry `high` / `medium` / `low`; (b) a **blind-spot scan** for unstated dependencies, omitted stakeholders, untested edge cases, and failure modes the premortem missed; (c) **alternative generation** of two distinct alternative approaches; and (d) a **trade-off analysis** comparing the proposed design against the two alternatives across cost, risk, complexity, and timeline. The gate is surfaced as a single multi-select decision with an explicit "Challenge nothing — write as-is" option, is advisory and **never blocks the write**, and runs even under `--continue`. Confidence ratings fold into the Assumptions entries in place; the blind spots, the two alternatives, and the trade-off comparison fold into a new optional `## Design challenge` section (not one of the seven hard-gated sections, and never surfaced in the round recap). Implemented in `skills/stride-ideation/SKILL.md` (the `## Challenge gate` section) and `skills/stride-ideation-ideate/SKILL.md` (the Step-5 enforcement bullet and the Step-6 confidence-rated Assumptions + `## Design challenge` template).
- **Challenge-gate documentation, fixture, and tests (W1371)** — a "Session experience" subsection in `README.md` documenting the gate's four components, placement, and advisory semantics; a catalogued calibration fixture `fixtures/2026-05-12T120300-saved-filters-challenge-gate-requirements.md` exhibiting a `## Design challenge` section (two alternatives + a cost/risk/complexity/timeline trade-off table) and confidence-rated Assumptions; a new `Stage 6: challenge-gate fixture shape` check in `lib/run_smoke_test.sh` + `lib/run_smoke_test.ps1` (the bash smoke test now reports **14 passed, 0 failed**); and a standalone `lib/test-challenge-gate.{sh,ps1}` suite (8 assertions each — four positive shape checks plus three negative controls guarding against vacuous passes). `fixtures/README.md` gains a section-4 catalogue entry, and two stale `stride-copilot-ideation` / `Copilot` references are corrected to `stride-codex-ideation` / `Codex CLI`.

### Source

Challenge-gate port: W1370 (skill + ideate surface), W1371 (README + fixtures + tests), W1372 (this release).

## [0.1.1] - 2026-06-22

### Fixed

- **The installer no longer overwrites an existing `AGENTS.md`.** Previously `install.sh` (line 83, project mode) and `install.ps1` unconditionally copied the bundle `AGENTS.md` over the destination, silently destroying a user-authored `AGENTS.md`. The installer now confines its content to an idempotent, clearly delimited managed block (`<!-- BEGIN stride-ideation -->` … `<!-- END stride-ideation -->`): a fresh file is created with the block; an existing file keeps all of its content with the block appended or refreshed in place; re-running the installer never duplicates the guidance; and a malformed/orphaned marker appends rather than truncating, so user content is never lost. The same guard is applied in both project and global mode and in both `install.sh` and `install.ps1` (verified byte-for-byte identical across fresh, existing, re-run, no-trailing-newline, and malformed-marker scenarios). The destination is only ever pattern-matched, never sourced or evaluated. Mirrors the [`stride-opencode-ideation` v0.2.1](https://github.com/cheezy/stride-opencode-ideation/releases/tag/v0.2.1) fix.

## [0.1.0] - 2026-05-27

Initial release — port of [`cheezy/stride-ideation`](https://github.com/cheezy/stride-ideation) (Claude Code) to Codex CLI, via the [`cheezy/stride-copilot-ideation`](https://github.com/cheezy/stride-copilot-ideation) port as the closest analog.

### Added

- **Repo scaffold (W880)** — `.codex-plugin/plugin.json` (name `stride-codex-ideation`, version `0.1.0`, Codex skills pointer to `./skills/`), MIT `LICENSE` (byte-identical to upstream `stride-ideation/LICENSE`), `.gitignore` (byte-identical to `stride-copilot-ideation/.gitignore`, excluding `.stride_auth.md` and `.stride/` runtime state), empty `skills/` / `agents/` / `lib/` / `fixtures/` subdirectories with a `fixtures/.gitkeep` placeholder. No git init in the new directory — it stays a plain subtree of the kanban worktree.
- **`skills/stride-ideation/SKILL.md` (W881)** — protocol skill ported from `stride-copilot-ideation/skills/stride-ideation/SKILL.md` with platform-neutral adaptations. The four `AskUserQuestion` tool-name references replaced with "interactive question batch (a single chat turn asking the user up to four related questions at once)" so the round-structure contract no longer depends on a Claude-Code-specific tool. The `preview`-pane paragraph rewritten as Codex-appropriate inline-ASCII guidance (Codex CLI has no first-class preview pane tool). The hard-gate logic (seven required sections + three shape requirements on Assumptions ranking and Success Metrics indicator types), four profiles (lean / product / discovery / lean-startup), round structure (1–6+ including the mandatory round-3 framing checkpoint, round-4 premortem, and lean-startup-only round-5 MVP-design), optional auxiliary sections, terminal state contract, and filename-helper deferral all preserved verbatim from the copilot port (line count identical at 165).
- **`skills/stride-ideation-ideate/SKILL.md` (W881)** — surface skill (218 lines, line-count identical to copilot source). Frontmatter description updated to "Codex CLI port of the upstream /stride-ideation:ideate command"; Step 3 plugin-root reference updated from `stride-copilot-ideation` to `stride-codex-ideation`. All eleven steps preserved verbatim, including the load-bearing safety checks: `--profile` validation, `--continue` validation, HARD INVARIANT (`TARGET_PATH ≠ CONTINUE_PATH`), seven hard-gated sections list, Decomposition seams + downstream-consumer paragraph, six MVP/Validation experiment sub-fields, terminal-state contract.
- **`skills/stride-ideation-stridify/SKILL.md` (W881)** — surface skill (651 lines, line-count identical to copilot source). Same Codex CLI framing swap; plugin-root references updated; auth-preflight paragraph clarified ("Codex CLI does not set a dedicated project-dir env var, so fall back to `$PWD` whenever `$CLAUDE_PROJECT_DIR` is unset"). All twelve steps preserved verbatim, including the four-layer resilience model (preflight advisory → per-goal partitioning → bounded 3-attempt retry → retry-exhaustion fallback writing the prompt to a sibling `*-decomposer-prompt.md` file), the 5-named-check validator, defensive `source_spec` + `source_spec_sha256` overwrite, and the never-retry-POST contract.
- **`agents/requirements-decomposer.md` and `agents/requirements-reviewer.md` (W882)** — both subagents ported to Codex's bare `.md` naming convention (NOT the copilot `.agent.md` form). Decomposer file copied byte-identical from the copilot source. Reviewer file has one substitution on line 23: "You may use the `Read` and `Grep` tools" → "You may use the `read` and `search` tools" (Codex vocabulary). The decomposer system prompt + chain-of-thought + JSON output rules + per-task allow-list schema, and the reviewer rubric (seven hard-gated sections + three shape requirements + profile-aware checks) are otherwise preserved verbatim.
- **`lib/` helpers (W883)** — twenty-four files copied byte-identical from `stride-copilot-ideation/lib/` using `cp -a` (executable bit on `.sh` files preserved). Eight cross-platform helpers (`drift_check.py`, `filename.sh` + `filename.ps1`, `read_auth.py`, `run_smoke_test.sh` + `run_smoke_test.ps1`, `strip_audit_fields.py`, `validate_batch.py`) plus sixteen `test-*` helpers in matching `.sh` / `.ps1` pairs. The bash smoke test reports **10 passed, 0 failed** when run from inside the new plugin directory.
- **`fixtures/` (W883)** — eight files copied byte-identical from `stride-copilot-ideation/fixtures/`: three `*-requirements.md` + `*-stride-batch.json` calibration pairs (dark-mode-toggle, notifications-system, replace-test-suite), `README.md`, `SMOKE-TEST-NOTE.md`, and the existing `.gitkeep` placeholder retained.
- **`AGENTS.md` and `README.md` (W884)** — top-level docs. `AGENTS.md` (66 lines) mirrors the `stride-codex/AGENTS.md` structural pattern: skill-activation table covering all three ideation skills, custom-agents section naming both subagents with their bare-`.md` filenames, workflow-sequence ASCII diagram, API-authorization paragraph, and the six-row Tool Name Mapping table identical to `stride-codex/AGENTS.md`. `README.md` (164 lines) adapted from `stride-copilot-ideation/README.md` with Codex-specific install instructions (one-liner via `install.sh`, PowerShell variant via `install.ps1`, manual `git clone` + `cp -r`), upstream-comparison table updated for Codex idioms, and sibling-plugin section retargeted at `stride-codex` (lifecycle).
- **`install.sh`, `install.ps1`, and this `CHANGELOG.md` (W885)** — bash and PowerShell installers patterned after `stride-codex/install.{sh,ps1}` but with the ideation payload (3 skills, 2 agents, 24 lib/ files, 8 fixtures/ files) instead of the lifecycle payload. Both installers support `--project` / `-Project` for project-local install and default to `~/.agents/` / `$env:USERPROFILE\.agents\` for global install. The bash variant uses `cp -a` to preserve the executable bit on `lib/*.sh`. Initial `CHANGELOG.md` entry documents the port and traces every file back to its W-identifier.

### Notes on the port

- **Skill activation language.** Where upstream Claude Code uses "Run `/stride-ideation:ideate`", this plugin uses "Activate the `stride-ideation-ideate` skill" — same substance, different invocation grammar. Codex CLI has no slash-command mechanism.
- **Agent naming.** The two subagents use the bare `<name>.md` convention (e.g. `agents/requirements-decomposer.md`), matching the existing `stride-codex/agents/` pattern. The Copilot port's `<name>.agent.md` form is NOT used here — Codex does not recognize the `.agent` infix.
- **Tool name generalization.** References to Claude Code tool names (`Read`, `Grep`, `Bash`, `Edit`, `Write`) in skill and agent bodies are translated to Codex equivalents (`read`, `search`, `shell`, `edit`, `write`) per the Tool Name Mapping table in `AGENTS.md`. Bash snippets that genuinely invoke `grep` etc. as shell commands are preserved verbatim (per the explicit "do NOT replace tool names inside literal command examples" pitfall on W881 and W882).
- **PowerShell mirrors.** Every bash helper and test file ships with a `.ps1` mirror so Windows Codex users get parity. The PowerShell code uses the same W505-reviewer-approved patterns the Copilot port introduced (`Set-StrictMode Latest`, `@()` wrap for `Get-Content`, `-notcontains` for negation, no `Write-Output` pollution into return channels). PowerShell runtime verification of the installer deferred — `pwsh` is not installed on the porting host; the same pattern was used in the reviewer-approved Copilot and Gemini PowerShell mirrors.
- **Upstream behavior preserved.** Every load-bearing safety check — `--profile` validation, `--continue` validation, HARD INVARIANT, seven-section gate, retry classifier, retry-exhaustion prompt-on-disk, defensive `source_spec` overwrite, never-retry-POST, source-doc read-only — is preserved verbatim. The fixtures (which are calibration references) and the decomposer/reviewer agent bodies are byte-identical to the copilot source (and via that, to upstream) so a regression run against the same requirements docs produces structurally identical batch JSON.
- **Not on a marketplace.** `stride-codex-ideation` is **not** published on `stride-marketplace` (which only carries the Claude Code version). Install via the direct GitHub URL using the one-liner in the README.

### Source

G143 (parent goal) / W880 (scaffold), W881 (skills), W882 (agents), W883 (lib + fixtures + smoke test), W884 (AGENTS.md + README.md), W885 (this CHANGELOG + install scripts).
